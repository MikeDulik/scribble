<h2>Comments</h2>

<ul>
  <% @comments.each do |comment| %>
    <li>
      <%= link_to "#{comment.title} - #{comment.post.name}",post_comment_path(comment.post,comment) %>
    </li>
  <% end %>
</ul>
